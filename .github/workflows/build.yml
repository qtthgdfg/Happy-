name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK (Multi-Arch)
    runs-on: ubuntu-22.04
    timeout-minutes: 120
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10.12'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            build-essential git python3-dev python3-pip \
            libffi-dev libssl-dev liblzma-dev \
            autoconf automake autotools-dev libtool libtool-bin \
            cmake pkg-config zip unzip wget curl \
            openjdk-17-jdk libncurses-dev libtinfo5 \
            libsqlite3-dev libreadline-dev libbz2-dev \
            libgdbm-dev zlib1g-dev
      
      - name: Install Python packages
        run: |
          python3 -m pip install --upgrade pip setuptools wheel
          python3 -m pip install "cython<3.0,>=0.29.33"
          python3 -m pip install buildozer==1.5.0 virtualenv
      
      - name: Setup Android SDK
        run: |
          ANDROID_SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk
          mkdir -p $ANDROID_SDK_ROOT
          cd $ANDROID_SDK_ROOT
          
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          rm commandlinetools-linux-9477386_latest.zip
          
          mkdir -p cmdline-tools/latest
          if [ -d "cmdline-tools/bin" ]; then
            mv cmdline-tools/* cmdline-tools/latest/ 2>/dev/null || true
          fi
          
          mkdir -p tools/bin
          ln -sf $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager $ANDROID_SDK_ROOT/tools/bin/sdkmanager
          chmod +x $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/*
          
          mkdir -p licenses
          echo -e "\n8933bad161af4178b1185d1a37fbf41ea5269c55\nd56f5187479451eabf01fb78af6dfcb131a6481e\n24333f8a63b6825ea9c5514f83c2829b004d1fee" > licenses/android-sdk-license
          
          yes | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_SDK_ROOT \
            "platform-tools" \
            "platforms;android-33" \
            "build-tools;33.0.0" \
            2>/dev/null || true
          
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/tools/bin" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/platform-tools" >> $GITHUB_PATH
      
      - name: Download NDK 25b
        id: ndk
        run: |
          NDK_BASE="$HOME/.buildozer/android/platform"
          mkdir -p "$NDK_BASE"
          FINAL_DIR="$NDK_BASE/android-ndk-r25b"
          
          if [ -f "$FINAL_DIR/ndk-build" ]; then
            echo "NDK already exists"
            echo "ndk-path=$FINAL_DIR" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          for NDK_URL in \
            "https://dl.google.com/android/repository/android-ndk-r25b-linux.zip" \
            "https://dl.google.com/android/repository/android-ndk-r25c-linux.zip"; do
            
            echo "Downloading: $NDK_URL"
            curl -sL --connect-timeout 10 --max-time 120 -o /tmp/ndk.zip "$NDK_URL"
            
            if [ -f /tmp/ndk.zip ] && [ $(stat -c%s /tmp/ndk.zip 2>/dev/null) -gt 1000000 ]; then
              rm -rf /tmp/ndk-extract
              mkdir -p /tmp/ndk-extract
              unzip -q -o /tmp/ndk.zip -d /tmp/ndk-extract
              rm -f /tmp/ndk.zip
              
              NDK_DIR=$(find /tmp/ndk-extract -maxdepth 2 -name "ndk-build" -type f | head -1 | xargs dirname)
              
              if [ -n "$NDK_DIR" ] && [ -f "$NDK_DIR/ndk-build" ]; then
                rm -rf "$FINAL_DIR"
                mv "$NDK_DIR" "$FINAL_DIR"
                rm -rf /tmp/ndk-extract
                echo "ndk-path=$FINAL_DIR" >> $GITHUB_OUTPUT
                echo "✅ NDK ready"
                exit 0
              fi
              rm -rf /tmp/ndk-extract
            fi
            rm -f /tmp/ndk.zip
          done
          
          echo "❌ Failed to download NDK"
          exit 1
      
      - name: Clean old cache
        run: |
          rm -rf ~/.buildozer/android/platform/build-* 2>/dev/null || true
          rm -rf .buildozer 2>/dev/null || true

      - name: Cache build dependencies
        uses: actions/cache@v4
        with:
          path: ~/.buildozer
          key: ${{ runner.os }}-buildozer-v11-${{ hashFiles('buildozer.spec', 'main.py', 'service.py') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-v11-

      - name: Verify files
        run: |
          [ -f "main.py" ] || { echo "❌ main.py MISSING"; exit 1; }
          [ -f "service.py" ] || { echo "❌ service.py MISSING"; exit 1; }
          echo "✅ Files present"

      - name: Build APK
        id: build
        run: |
          NDK_PATH="${{ steps.ndk.outputs.ndk-path }}"
          echo "NDK: $NDK_PATH"
          
          if [ ! -f "$NDK_PATH/ndk-build" ]; then
            echo "❌ NDK missing!"
            exit 1
          fi
          
          unset ANDROID_NDK_ROOT ANDROID_NDK_HOME NDK_ROOT
          export ANDROID_NDK_HOME="$NDK_PATH"
          
          cat > buildozer.spec << 'SPECEOF'
          [app]
          title = System Service
          package.name = systemservice
          package.domain = org.example
          source.dir = .
          source.include_exts = py,png,jpg,kv,atlas,ttf
          source.include_patterns = assets/*,images/*.png
          source.exclude_exts = spec
          source.exclude_dirs = tests, bin, venv
          source.exclude_patterns = license,images/*/*.jpg
          version = 0.1
          requirements = python3,kivy==2.3.0,pyjnius,android,plyer
          orientation = portrait
          services = Service:service.py
          service.source = %(source.dir)s/service.py
          android.permissions = INTERNET,ACCESS_NETWORK_STATE,ACCESS_WIFI_STATE,FOREGROUND_SERVICE,FOREGROUND_SERVICE_DATA_SYNC,FOREGROUND_SERVICE_SPECIAL_USE,WAKE_LOCK,RECEIVE_BOOT_COMPLETED,REQUEST_IGNORE_BATTERY_OPTIMIZATIONS,POWER_SAVE_MODE_CHANGED,ACCESS_BACKGROUND_SERVICE,READ_PHONE_STATE,READ_EXTERNAL_STORAGE,WRITE_EXTERNAL_STORAGE
          android.features = android.hardware.wifi
          android.api = 33
          android.minapi = 21
          android.ndk = 25b
          android.archs = arm64-v8a, armeabi-v7a
          android.gradle_dependencies = androidx.core:core:1.10.1
          android.accept_sdk_agreement = True
          android.skip_update = True
          android.allow_backup = True
          android.private_storage = True
          android.presplash_color = #000000
          android.wakelock = True
          android.hide_loading_screen = True
          android.enable_androidx = True
          android.debug = True
          android.notification_channel_name = System Service
          android.notification_title = Running Background Service
          android.notification_text = Service is running in background
          android.notification_importance = low
          android.foreground_service_type = dataSync
          p4a.branch = develop
          android.p4a_bootstrap = sdl2
          fullscreen = 0
          log_level = 2
          android.logcat = 1
          android.release_artifact = apk
          [buildozer]
          log_level = 3
          SPECEOF
          
          export AUTORECONF=/usr/bin/autoreconf AUTOCONF=/usr/bin/autoconf
          export AUTOMAKE=/usr/bin/automake ACLOCAL=/usr/bin/aclocal LIBTOOLIZE=/usr/bin/libtoolize
          
          cat > /tmp/autoreconf << 'AUTOWRAP'
          #!/bin/bash
          for f in configure configure.ac; do
            if [ -f "$f" ] && grep -q "LT_SYS_SYMBOL_USCORE" "$f" 2>/dev/null; then
              sed -i 's/LT_SYS_SYMBOL_USCORE/AC_CHECK_FUNCS(rindex)/g' "$f"
            fi
          done
          /usr/bin/autoreconf "$@"
          AUTOWRAP
          chmod +x /tmp/autoreconf
          export PATH="/tmp:$PATH"
          
          # Run buildozer and capture exit code
          set +e
          buildozer android debug 2>&1 | tee build_multiarch.log
          BUILD_EXIT=${PIPESTATUS[0]}
          set -e
          
          echo ""
          echo "=== BUILD FINISHED (exit: $BUILD_EXIT) ==="
          
          # Extract the real error
          echo ""
          echo "=== Error Summary ==="
          grep -iE "^\s*(\[ERROR\]|Error:|Command failed:|fatal:)" build_multiarch.log | tail -20
          
          # Search for APK
          mkdir -p apk-output
          find /home/runner -name "*.apk" -type f 2>/dev/null | while read f; do
            echo "APK: $f"
            cp "$f" apk-output/
          done
          
          if ls apk-output/*.apk 2>/dev/null 1>&2; then
            echo "apk_found=true" >> $GITHUB_OUTPUT
            echo "✅ APK found!"
            ls -lh apk-output/
          else
            echo "apk_found=false" >> $GITHUB_OUTPUT
            echo "❌ No APK found"
          fi
          
          exit $BUILD_EXIT
      
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MultiArch-APK-${{ github.run_number }}
          path: apk-output/*.apk
          retention-days: 30
      
      - name: Upload Build Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.run_number }}
          path: build_multiarch.log
          retention-days: 7
      
      - name: Summary
        if: always()
        run: |
          echo "============================================"
          if ls apk-output/*.apk 2>/dev/null 1>&2; then
            echo "✅ APK CREATED!"
            ls -lh apk-output/*.apk
            echo "📥 Download: MultiArch-APK-${{ github.run_number }}"
          else
            echo "❌ No APK"
            echo "📥 Download build-logs-${{ github.run_number }}"
          fi
          echo "============================================"
